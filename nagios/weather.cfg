# 'check_weather' command definition
define command {
        command_line                   /usr/lib/nagios/plugins/check_weather -w 0 -c 0 -s '$ARG1$'
        command_name                   check_weather
        }
